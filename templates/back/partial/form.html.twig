{% extends 'back/base.html.twig' %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block title %}
{{ title }}
{% endblock %}

{% block body %}

    <div class="container-fluid d-flex flex-column">

        <a  class="mybtn my-3 float-end"
            href="{{ path(link) }}"
        >
            Retour aux {{ liste }}
        </a>

        <div>
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>

    </div>

{% endblock %}